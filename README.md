# content-moderation

Benchmark message moderation with Bun using three different approaches:

- A simple rule-based filter list
- An embedding model
- A small LLM

## Getting started

To install dependencies:

```bash
bun install
```

To run server:

```bash
bun server
```

To run client:

```bash
bun client
```
